import UAParser from "ua-parser-js";

export const uaParser = new UAParser();

// show the uaParser infomation by
// log.log(uaParser.getResult());

// OS: [Windows, Mac OS, iOS, Android, WebOS, Ubuntu, Debian, SUSE, Linux, Solaris, UNIX]
